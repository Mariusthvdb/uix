id: forge_spark_map_entity_filter_show_all
description: "Map spark in entity_filter mode works with show_all: true"
view_path: forge-spark-map-entity-filter-show-all

setup:
  - type: ha_service
    domain: device_tracker
    service: see
    data:
      dev_id: uix_test_person
      gps:
        - 51.5074
        - -0.1278
      gps_accuracy: 1
  - type: ha_service
    domain: device_tracker
    service: see
    data:
      dev_id: uix_test_person_slider
      gps:
        - 51.5220
        - -0.1280
      gps_accuracy: 1
  - type: ha_service
    domain: device_tracker
    service: see
    data:
      dev_id: uix_tracker_one
      gps:
        - 51.5200
        - -0.1300
      gps_accuracy: 1
  - type: ha_service
    domain: device_tracker
    service: see
    data:
      dev_id: uix_tracker_two
      gps:
        - 51.5210
        - -0.1295
      gps_accuracy: 1
  - type: wait
    ms: 2000

card:
  type: custom:uix-forge
  forge:
    mold: card
    sparks:
      - type: map
        entity_filter: true
  element:
    type: map
    show_all: true

interactions:
  - type: wait
    ms: 3000
  - type: click
    root:
      - hui-map-card
      - ha-map
    selector: ha-button
    settle_ms: 2000

assertions:
  - type: element_present
    root:
      - hui-map-card
      - ha-map
    selector: div.uix-map-entity-filter-control

  - type: element_present
    root:
      - hui-map-card
      - ha-map
    selector: ha-dropdown

  # The "Show All" master toggle checkbox should exist
  - type: element_present
    root:
      - hui-map-card
      - ha-map
    selector: "ha-dropdown-item[data-show-all-toggle]"

  - type: text_equals
    root:
      - hui-map-card
      - ha-map
    selector: "ha-dropdown-item[data-show-all-toggle]"
    expected: "Show All"

  - type: snapshot
    name: "30_forge_spark_map_entity_filter_show_all"
    threshold: 0.05